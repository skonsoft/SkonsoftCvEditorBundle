<h1>CvUploadedDocument</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Original</th>
            <td>{{ entity.original }}</td>
        </tr>
        <tr>
            <th>Filetype</th>
            <td>{{ entity.fileType }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('cv_upload') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cv_upload_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('cv_upload_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
