<h1>CvUploadedDocument list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Original</th>
            <th>Filetype</th>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('cv_upload_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.original }}</td>
            <td>{{ entity.fileType }}</td>
            <td>{{ entity.name }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('cv_upload_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cv_upload_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('cv_upload_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
