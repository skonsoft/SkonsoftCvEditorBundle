<h1>CvDocument creation</h1>

<form action="{{ path('cv_document_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cv_document') }}">
            Back to the list
        </a>
    </li>
</ul>
