<h1>CvDocument list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Document</th>
            <th>Annotateddocument</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('cv_document_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.document }}</td>
            <td>{{ entity.annotatedDocument }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('cv_document_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cv_document_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('cv_document_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
